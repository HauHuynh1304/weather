import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  container: {
    borderColor: '#ad9f9e',
    borderRadius: 10,
  },
  textInput: {
    fontSize: 20,
    color: '#fff',
  },
});

export default styles;
